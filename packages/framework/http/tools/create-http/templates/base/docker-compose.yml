services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: inversify
      POSTGRES_PASSWORD: inversify
      POSTGRES_USER: inversify
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
