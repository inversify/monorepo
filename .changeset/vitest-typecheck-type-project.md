---
"@inversifyjs/foundation-vitest-config": patch
---

Enabled explicit Vitest typecheck on the Type project so `*.spec-d.ts` files are type-checked and `vitest run` for that project only runs those type tests.
